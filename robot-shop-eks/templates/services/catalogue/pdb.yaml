{{- if .Values.catalogue.pdb.enabled }}

apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: 
  name: catalogue-pdb
  namespace: {{ .Values.namespace }}

spec: 
  maxUnavailable: {{ .Values.catalogue.pdb.maxUnavailable }}

  selector: 
    matchLabels:
      app: catalogue

{{- end }}